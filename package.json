{
  "name": "building-robust-data-pipelines",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Reveal.js talk deck: Building robust data pipelines (YAUG)",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "audit": "npm audit --audit-level=high"
  },
  "dependencies": {
    "@fontsource/dm-sans": "^5.3.0",
    "reveal.js": "^5.2.1"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
